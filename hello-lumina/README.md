# Test Project for Lumina VS Code Extension

Try these VS Code commands:

1. Select code → `Ctrl+Alt+E` to explain
2. Place cursor → `Ctrl+Alt+G` to generate
3. `Ctrl+Alt+L` to open chat
4. Open file → Lumina: Review
